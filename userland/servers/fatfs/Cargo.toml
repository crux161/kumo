[package]
name = "fatfs"
authors.workspace = true
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
kumo-fatfs = { path = "../../../lib/kumo-fatfs" }
kumo-vfs = { path = "../../../lib/kumo-vfs" }

[lib]
test = true

