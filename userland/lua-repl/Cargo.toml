#j485
[package]
name = "lua-repl"
version = "0.1.0"
edition = "2021"

[features]
default = []
piccolo-vm = ["dep:piccolo"]

[dependencies]
kumo-abi = { path = "../../lib/kumo-abi" }
kumo-rt = { path = "../kumo-rt" }
piccolo = { version = "0.3.3", optional = true }
